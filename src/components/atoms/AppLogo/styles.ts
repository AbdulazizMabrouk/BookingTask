import {StyleSheet} from 'react-native';
import {calcHeight, calcWidth} from '../../../utils';
export default StyleSheet.create({
  logo: {
    height: calcWidth(97.7),
    width: calcHeight(125.3),
  },
});
